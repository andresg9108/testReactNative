import {
    StyleSheet
} from 'react-native'




const styles = StyleSheet.create({
    container: {
        flex: 1,
        width: '100%',
        backgroundColor: '#000'
    },
    containerText: {
        fontSize: 25,
        color: '#fff'
    }
})

export default styles