import {
    StyleSheet
} from 'react-native'
import {
    fontSize, 
    colorPalette 
} from '../../../../mainStyles'




const styles = StyleSheet.create({
    icon: {
        width: 35, 
        height: 35, 
        marginVertical: 5, 
    }
})

export default styles