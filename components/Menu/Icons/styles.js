import {
    StyleSheet
} from 'react-native'
import {
    fontSize, 
    colorPalette 
} from '../../../mainStyles'




const styles = StyleSheet.create({
    container: {
        width: 50,
        flexDirection: 'column', 
        justifyContent: 'center', 
        alignItems: 'center', 
        alignContent: 'center' 
    }
})

export default styles