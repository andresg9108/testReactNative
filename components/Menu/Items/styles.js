import {
    StyleSheet
} from 'react-native'
import {
    fontSize, 
    colorPalette 
} from '../../../mainStyles'




const styles = StyleSheet.create({
    container: {
        flex: 1, 
        flexDirection: 'column', 
        justifyContent: 'center', 
        alignItems: 'center', 
        alignContent: 'center' 
    }
})

export default styles